# 📸 ФОТО ОБ'ЄКТІВ ДЛЯ ЗВІТНОСТІ - ДОКУМЕНТАЦІЯ

## 🎯 Огляд

Додано можливість для менеджерів завантажувати необмежену кількість фото для кожного об'єкта. Ці фото призначені для звітності клієнтам та документування стану об'єкта.

**Відмінність від фото процесів:**
- **Фото процесів** 📷 - додають працівники (До/Під час/Після роботи)
- **Фото об'єктів** 🏢 - додають менеджери (загальний вигляд, локація, звітність)

---

## 🏗️ Технічна Реалізація

### 1. База Даних

**Файл:** `/app/supabase/migrations/008_add_object_photos.sql`

**Структура:**
```sql
ALTER TABLE objects
ADD COLUMN IF NOT EXISTS photos JSONB DEFAULT '[]'::jsonb,
ADD COLUMN IF NOT EXISTS photos_updated_at TIMESTAMPTZ DEFAULT NOW();
```

**Формат JSONB:**
```json
[
  {
    "url": "data:image/jpeg;base64,...",
    "label": "Фасад будівлі",
    "uploadedBy": "manager-user-id",
    "uploadedAt": "2024-01-15T10:30:00Z"
  },
  {
    "url": "data:image/jpeg;base64,...",
    "label": "Інтер'єр приміщення",
    "uploadedBy": "manager-user-id",
    "uploadedAt": "2024-01-15T11:00:00Z"
  }
]
```

**Функції:**
- `object_photos_count(object_id)` - кількість фото
- `object_has_photos(object_id)` - чи є фото
- `add_object_photo(object_id, url, label, user_id)` - додати фото
- `remove_object_photo(object_id, index)` - видалити фото
- `get_object_photos_statistics(manager_id)` - статистика
- `get_all_object_photos(manager_id)` - всі фото всіх об'єктів

**View:**
- `objects_with_photos` - всі об'єкти з хоча б одним фото

---

### 2. TypeScript Інтерфейси

**Файл:** `/app/src/types/index.ts`

```typescript
export interface ObjectPhoto {
  url: string;          // Base64 зображення
  label: string;        // Підпис до фото
  uploadedBy: string;   // ID користувача
  uploadedAt: string;   // Час завантаження ISO
}

export interface ObjectType {
  // ... існуючі поля
  photos?: ObjectPhoto[];     // Масив фото
  photosUpdatedAt?: string;   // Час останнього оновлення
}
```

---

### 3. Компоненти

#### 🎨 ObjectPhotosManager (новий)

**Файл:** `/app/src/components/manager/ObjectPhotosManager.tsx`

**Призначення:** Керування фото конкретного об'єкта

**Функції:**
- ✅ Додавання необмеженої кількості фото
- ✅ Підпис до кожного фото
- ✅ Редагування підписів
- ✅ Видалення фото
- ✅ Попередній перегляд (галерея)
- ✅ Збереження всіх змін

**Props:**
```typescript
interface ObjectPhotosManagerProps {
  open: boolean;
  onClose: () => void;
  object: ObjectType;                    // Об'єкт для роботи
  onSave: (photos: ObjectPhoto[]) => Promise<void>;
}
```

**UI:**
```
┌────────────────────────────────────────┐
│ 📸 Фото Об'єкта: Квартира №15         │
├────────────────────────────────────────┤
│ ┌──────────────────────────────────┐  │
│ │ 🏢 Фото об'єкта: 3                │  │
│ │ 📍 вул. Шевченка, 25              │  │
│ └──────────────────────────────────┘  │
│                                        │
│ [➕ Додати Нове Фото]                 │
│                                        │
│ Фото (3):                              │
│ ┌──────────────────────────────────┐  │
│ │ [img] 1. Фасад будівлі            │  │
│ │       15.01.2024, 10:30           │  │
│ │       [✏️ Редагувати] [🗑️ Видалити]│  │
│ ├──────────────────────────────────┤  │
│ │ [img] 2. Інтер'єр                 │  │
│ │       15.01.2024, 11:00           │  │
│ │       [✏️ Редагувати] [🗑️ Видалити]│  │
│ └──────────────────────────────────┘  │
│                                        │
│ Попередній перегляд:                   │
│ ┌────┬────┬────┐                       │
│ │ 📷 │ 📷 │ 📷 │                       │
│ └────┴────┴────┘                       │
│                                        │
│ [Скасувати]    [💾 Зберегти Всі Фото] │
└────────────────────────────────────────┘
```

---

#### 🔄 ManageObjectsModal (оновлено)

**Файл:** `/app/src/components/manager/ManageObjectsModal.tsx`

**Додано:**
- ✅ Кнопка "📸 Фото (N)" для кожного об'єкта
- ✅ Лічильник фото в дужках
- ✅ Інтеграція з ObjectPhotosManager
- ✅ Функція збереження фото

**Кнопка:**
```tsx
<Button
  size="sm"
  variant="outline"
  onClick={() => setPhotosObject(obj)}
  className="text-xs bg-indigo-50 text-indigo-700"
>
  <Camera className="w-3 h-3 mr-1" />
  Фото {obj.photos && obj.photos.length > 0 && `(${obj.photos.length})`}
</Button>
```

**Розміщення кнопки:**
```
[📊 Звіт] [📦 Матеріали] [📸 Фото (3)] [✏️ Редагувати] [🗑️ Видалити]
```

---

## 📱 Використання

### Для Менеджерів:

#### 1. Додати Фото до Об'єкта

**Крок 1:** Відкрийте "Керування Об'єктами"

**Крок 2:** Знайдіть потрібний об'єкт

**Крок 3:** Натисніть кнопку "📸 Фото"

**Крок 4:** У модальному вікні натисніть "➕ Додати Нове Фото"

**Крок 5:** Виберіть спосіб:
- 📷 **Камера** - миттєва зйомка
- 📁 **Вибрати** - з галереї/диска

**Крок 6:** Додайте підпис до фото:
- "Фасад будівлі"
- "Інтер'єр квартири"
- "Загальний вигляд об'єкта"
- "План поверху"
- тощо

**Крок 7:** Натисніть "Додати"

**Крок 8:** Повторіть для інших фото (необмежена кількість)

**Крок 9:** Натисніть "💾 Зберегти Всі Фото"

---

#### 2. Редагувати Підпис

**Крок 1:** Відкрийте фото об'єкта

**Крок 2:** Знайдіть потрібне фото в списку

**Крок 3:** Натисніть "✏️ Редагувати"

**Крок 4:** Змініть підпис

**Крок 5:** Натисніть "Зберегти"

---

#### 3. Видалити Фото

**Крок 1:** Відкрийте фото об'єкта

**Крок 2:** Знайдіть потрібне фото

**Крок 3:** Натисніть "🗑️ Видалити"

**Крок 4:** Фото видалено з масиву

**Крок 5:** Натисніть "💾 Зберегти Всі Фото" для збереження змін

---

#### 4. Переглянути Фото

**В модальному вікні:**
- Мініатюри показані в списку (ліворуч)
- Галерея для перегляду (внизу)

**Клік на мініатюру в галереї:**
- Повноекранний режим
- Навігація стрілками ←→
- ESC для закриття

---

## 🎯 Сценарії Використання

### Сценарій 1: Презентація Об'єкта Клієнту

**Менеджер Олена:**

1. Перед початком робіт:
   - Додає фото "Загальний вигляд об'єкта"
   - Додає фото "Поточний стан приміщення"
   - Додає фото "Проблемні зони"

2. Під час роботи:
   - Додає фото прогресу
   - Оновлює підписи з датами

3. Після завершення:
   - Додає фото результату
   - Створює звіт для клієнта
   - Показує порівняння До/Після

**Клієнт:**
- Бачить весь процес
- Розуміє за що платить
- Задоволений прозорністю

---

### Сценарій 2: Документування Локації

**Менеджер Іван:**

1. Новий об'єкт "Будинок на вул. Шевченка, 25"

2. Додає фото локації:
   - "Зовнішній вигляд будівлі"
   - "Під'їзд до об'єкта"
   - "Паркування"
   - "Вхід в приміщення"

3. Надає працівникам:
   - Показує як знайти об'єкт
   - Де паркуватись
   - Куди заходити

**Працівники:**
- Легко знаходять об'єкт
- Не дзвонять з питаннями
- Економлять час

---

### Сценарій 3: Звітність для Власника Бізнесу

**Менеджер (Власник бізнесу):**

1. Щомісячний звіт:
   - Переглядає всі об'єкти
   - Дивиться фото кожного
   - Оцінює прогрес

2. Портфоліо:
   - Збирає кращі фото
   - Створює презентацію
   - Показує потенційним клієнтам

3. Маркетинг:
   - Використовує фото на сайті
   - Публікує в соцмережах
   - Демонструє можливості

---

## 💡 Рекомендації

### Які Фото Додавати:

✅ **КОРИСНІ:**
- Загальний вигляд об'єкта
- Фасад будівлі
- Адреса та вхід
- Поточний стан (до початку робіт)
- Проблемні зони
- Особливості локації
- Паркування/під'їзд
- Результат робіт

❌ **НЕПОТРІБНІ:**
- Розмиті фото
- Фото особистих речей
- Конфіденційна інформація
- Погані ракурси

---

### Підписи до Фото:

✅ **ДОБРІ:**
- "Фасад будівлі (15.01.2024)"
- "Інтер'єр - поточний стан"
- "Проблемна зона: тріщини на стіні"
- "Результат ремонту"

❌ **ПОГАНІ:**
- "Фото 1"
- "IMG_1234"
- "Будинок"

---

## 📊 SQL Функції

### Додати Фото

```sql
SELECT add_object_photo(
  'object-id',
  'data:image/jpeg;base64,...',
  'Фасад будівлі',
  'manager-id'
);
```

### Видалити Фото

```sql
-- Видалити перше фото (індекс 0)
SELECT remove_object_photo('object-id', 0);
```

### Кількість Фото

```sql
SELECT object_photos_count('object-id');
-- Результат: 5
```

### Чи Є Фото

```sql
SELECT object_has_photos('object-id');
-- Результат: true/false
```

### Статистика

```sql
-- Загальна статистика
SELECT * FROM get_object_photos_statistics();

-- Для конкретного менеджера
SELECT * FROM get_object_photos_statistics('manager-id');

-- Результат:
-- total_objects: 10
-- objects_with_photos: 7
-- total_photos: 23
-- coverage_percent: 70.00
-- avg_photos_per_object: 3.29
```

### Всі Об'єкти з Фото

```sql
SELECT * FROM objects_with_photos
ORDER BY photos_updated_at DESC
LIMIT 10;
```

### Всі Фото Менеджера

```sql
SELECT * FROM get_all_object_photos('manager-id');

-- Результат:
-- object_id, object_name, object_address, photo_url, photo_label, uploaded_by, uploaded_at
```

---

## 🔐 Безпека (RLS)

### Політики:

**Перегляд:**
```sql
-- Менеджери бачать фото своїх об'єктів
-- Працівники бачать фото об'єктів де працюють
```

**Редагування:**
```sql
-- Тільки менеджери можуть додавати/видаляти фото
-- Тільки для своїх об'єктів
```

---

## ⚡ Оптимізація

### GIN Індекс для JSONB:

```sql
CREATE INDEX idx_objects_with_photos
ON objects USING GIN (photos)
WHERE jsonb_array_length(photos) > 0;
```

**Переваги:**
- Швидкий пошук об'єктів з фото
- Ефективні запити по JSONB
- Мінімальне навантаження

---

## 🚀 Майбутні Покращення

### 1. Експорт PDF Звіту

```
┌─────────────────────────────────┐
│ Об'єкт: Квартира №15            │
│ Адреса: вул. Шевченка, 25       │
│                                 │
│ [Фото 1: Фасад]                 │
│ [Фото 2: Інтер'єр]              │
│ [Фото 3: Результат]             │
│                                 │
│ Підпис менеджера: __________    │
└─────────────────────────────────┘
```

---

### 2. Відправка на Email

- Кнопка "Надіслати клієнту"
- Вибір фото для відправки
- Автоматичне форматування
- Email з PDF прикріпленим

---

### 3. Порівняння До/Після

- Інтерактивний слайдер
- Два фото поруч
- Візуальне порівняння

---

### 4. QR Code для Об'єкта

- Генерація QR коду
- Сканування = всі фото об'єкта
- Зручно для працівників

---

## ✅ Чеклист Запуску

- [ ] Виконати SQL міграцію `008_add_object_photos.sql`
- [ ] Протестувати додавання фото
- [ ] Протестувати редагування підписів
- [ ] Протестувати видалення фото
- [ ] Протестувати збереження
- [ ] Перевірити відображення лічильника
- [ ] Перевірити галерею

---

## 🎉 Результат

**Що отримали:**

✅ **Для менеджерів:**
- Необмежена кількість фото для кожного об'єкта
- Підписи для кожного фото
- Легке редагування та видалення
- Галерея для перегляду

✅ **Для бізнесу:**
- Документування всіх об'єктів
- Звітність для клієнтів
- Портфоліо робіт
- Маркетингові матеріали
- Прозорість процесів

✅ **Для клієнтів:**
- Візуальне підтвердження робіт
- Розуміння стану об'єкта
- Довіра до компанії

---

**🚀 Готово до використання!**

Фото об'єктів для звітності повністю реалізовані та інтегровані в систему!
