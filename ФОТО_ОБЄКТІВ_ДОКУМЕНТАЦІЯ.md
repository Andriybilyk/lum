# 🏗️ ФОТО ОБ'ЄКТІВ - ДОКУМЕНТАЦІЯ

## 🎯 Огляд

Менеджери тепер можуть додавати **фото об'єктів** для звітності клієнтам. Це окрема функція від фото процесів працівників.

### Різниця між фото процесів та фото об'єктів:

| Фото Процесів (Працівники) | Фото Об'єктів (Менеджери) |
|---------------------------|--------------------------|
| 📸 Працівники додають при виконанні процесу | 🏗️ Менеджери додають для об'єкта |
| 3 етапи: До/Під час/Після роботи | Необмежена кількість з підписами |
| Прив'язані до конкретного процесу | Прив'язані до об'єкта загалом |
| Для контролю якості робіт | Для звітності клієнтам |
| Зберігаються в `processes.photo_before/during/after` | Зберігаються в `objects.photos` (JSONB масив) |

---

## 📋 Створені Файли

### 1. SQL Міграція

**Файл:** `/app/supabase/migrations/008_add_object_photos.sql`

**Що додано:**
```sql
-- Поле для масиву фото (JSONB)
ALTER TABLE objects
ADD COLUMN IF NOT EXISTS photos JSONB DEFAULT '[]'::jsonb;

-- Час останнього оновлення
ADD COLUMN IF NOT EXISTS photos_updated_at TIMESTAMPTZ DEFAULT NOW();
```

**Структура JSONB:**
```json
[
  {
    "url": "data:image/jpeg;base64,...",
    "label": "Фасад будівлі",
    "uploadedBy": "manager-user-id",
    "uploadedAt": "2024-01-15T10:30:00Z"
  },
  {
    "url": "data:image/jpeg;base64,...",
    "label": "Інтер'єр приміщення",
    "uploadedBy": "manager-user-id",
    "uploadedAt": "2024-01-15T11:00:00Z"
  }
]
```

**Функції:**
- `object_photos_count(object_id)` - кількість фото
- `object_has_photos(object_id)` - чи є фото
- `add_object_photo(...)` - додати фото
- `remove_object_photo(object_id, index)` - видалити фото
- `get_object_photos_statistics()` - статистика
- `get_all_object_photos(manager_id)` - всі фото менеджера

**View:**
- `objects_with_photos` - об'єкти з фото

---

### 2. TypeScript Інтерфейси

**Файл:** `/app/src/types/index.ts`

```typescript
export interface ObjectPhoto {
  url: string;           // Base64 зображення
  label: string;         // Підпис до фото
  uploadedBy: string;    // ID менеджера
  uploadedAt: string;    // ISO дата
}

export interface ObjectType {
  // ... існуючі поля
  photos?: ObjectPhoto[];        // Масив фото
  photosUpdatedAt?: string;      // Час оновлення
}
```

---

### 3. Компоненти

#### 🎨 ObjectPhotosManager

**Файл:** `/app/src/components/manager/ObjectPhotosManager.tsx`

**Призначення:** Керування фото конкретного об'єкта

**Функції:**
- ✅ Додавання нових фото з підписом
- ✅ Редагування підписів
- ✅ Видалення фото
- ✅ Попередній перегляд галереї
- ✅ Зберігання всіх змін

**Використання:**
```tsx
<ObjectPhotosManager
  open={true}
  onClose={() => setPhotosObject(null)}
  object={selectedObject}
  onSave={handleSaveObjectPhotos}
/>
```

**Інтерфейс:**
```
┌─────────────────────────────────┐
│ 📸 Фото Об'єкта: Квартира №15  │
├─────────────────────────────────┤
│ Фото об'єкта: 3                │
│ 📍 вул. Шевченка, 10           │
│                                │
│ [+ Додати Нове Фото]           │
│                                │
│ Фото (3)                       │
│ ┌─────────────────────────┐   │
│ │ [img] 1. Фасад будівлі  │   │
│ │ 15.01.2024 10:30        │   │
│ │ [Редагувати] [Видалити] │   │
│ ├─────────────────────────┤   │
│ │ [img] 2. Інтер'єр       │   │
│ │ 15.01.2024 11:00        │   │
│ └─────────────────────────┘   │
│                                │
│ Попередній перегляд            │
│ [🖼️] [🖼️] [🖼️]               │
│                                │
│ [Скасувати] [💾 Зберегти Всі] │
└─────────────────────────────────┘
```

---

#### 🖼️ ObjectPhotosGallery

**Файл:** `/app/src/components/manager/ObjectPhotosGallery.tsx`

**Призначення:** Централізована галерея всіх фото об'єктів

**Функції:**
- ✅ Перегляд всіх фото з усіх об'єктів
- ✅ Фільтрація по об'єкту
- ✅ Статистика (всього фото, об'єктів, середньо)
- ✅ Повноекранний перегляд

**Інтерфейс:**
```
┌─────────────────────────────────┐
│ 📸 Галерея Фото Об'єктів       │
├─────────────────────────────────┤
│ ┌────┬────┬────┐               │
│ │ 45 │ 12 │ 4  │               │
│ │Фото│Об  │Сер │               │
│ └────┴────┴────┘               │
│                                │
│ 🔍 Фільтри                     │
│ Об'єкт: [▼ Всі об'єкти]       │
│ [Скинути]                      │
│                                │
│ Фото (45)                      │
│ ┌────┬────┬────┬────┐          │
│ │ 📷 │ 📷 │ 📷 │ 📷 │          │
│ └────┴────┴────┴────┘          │
└─────────────────────────────────┘
```

---

### 4. Оновлення Існуючих Компонентів

#### ManageObjectsModal

**Додано:**
- ✅ Кнопка "📸 Фото (X)" для кожного об'єкта
- ✅ Відображення кількості фото
- ✅ Стилізація кнопки (indigo колір)
- ✅ Функція `handleSaveObjectPhotos`

**Код кнопки:**
```tsx
<Button
  size="sm"
  variant="outline"
  onClick={() => setPhotosObject(obj)}
  className="text-xs bg-indigo-50 hover:bg-indigo-100"
>
  <Camera className="w-3 h-3 mr-1" />
  Фото {obj.photos && obj.photos.length > 0 && `(${obj.photos.length})`}
</Button>
```

#### ManagerStats

**Додано:**
- ✅ Кнопка "🏗️ Фото Об'єктів"
- ✅ Перейменовано "📸 Фото Звіти" → "📸 Фото Процесів"
- ✅ State `showObjectPhotos`
- ✅ Рендер `ObjectPhotosGallery`

**Кнопки:**
```tsx
// Фото процесів (працівники)
<Button onClick={() => setShowPhotoReports(true)}>
  <span>📸 Фото Процесів</span>
</Button>

// Фото об'єктів (менеджери)
<Button onClick={() => setShowObjectPhotos(true)}>
  <span>🏗️ Фото Об'єктів</span>
</Button>
```

---

## 📱 Інструкція Використання

### Для Менеджерів

#### 1. Додати фото до об'єкта

**Варіант A: Через "Керування Об'єктами"**

1. Відкрийте "Керування Об'єктами"
2. Знайдіть потрібний об'єкт
3. Натисніть кнопку "📸 Фото (X)"
4. Натисніть "+ Додати Нове Фото"
5. Оберіть спосіб:
   - 📷 **Камера** - миттєва зйомка
   - 📁 **Вибрати** - з галереї
6. Додайте підпис (наприклад: "Фасад", "Інтер'єр", "Загальний вигляд")
7. Натисніть "Додати"
8. Повторіть для інших фото
9. Натисніть "💾 Зберегти Всі Фото"

**Варіант B: Під час створення об'єкта**

1. Створіть новий об'єкт
2. Після створення натисніть "📸 Фото"
3. Додайте фото

---

#### 2. Редагувати підпис фото

1. Відкрийте "Керування Об'єктами"
2. Клік на "📸 Фото (X)" для об'єкта
3. Натисніть "Редагувати" біля потрібного фото
4. Змініть підпис
5. Натисніть "Зберегти"
6. "💾 Зберегти Всі Фото"

---

#### 3. Видалити фото

1. Відкрийте менеджер фото об'єкта
2. Натисніть "Видалити" біля потрібного фото
3. Фото зникне зі списку
4. "💾 Зберегти Всі Фото" для підтвердження

---

#### 4. Переглянути всі фото об'єктів

1. Меню менеджера → "🏗️ Фото Об'єктів"
2. Побачите статистику:
   - Всього фото
   - Об'єктів з фото
   - Середньо фото на об'єкт
3. Фільтруйте по об'єкту (dropdown)
4. Натисніть на фото для збільшення
5. Навігація стрілками ←→
6. ESC для закриття

---

## 🎨 Приклади Використання

### Сценарій 1: Звіт клієнту про прогрес

**Менеджер Олена:**

1. Об'єкт: "Ремонт офісу, БЦ Центральний"
2. Додає фото:
   - "Стан До початку робіт - зал"
   - "Стан До початку робіт - коридор"
   - "Демонтаж старого покриття"
   - "Вирівнювання стін"
   - "Покраска стін - 50%"
   - "Укладання ламінату"
   - "Завершений вигляд - зал"
   - "Завершений вигляд - коридор"

3. Відкриває "🏗️ Фото Об'єктів"
4. Фільтрує по "Ремонт офісу"
5. Бачить усі 8 фото в хронологічному порядку
6. Показує клієнту на екрані або робить скріншоти
7. Клієнт задоволений прозорістю

---

### Сценарій 2: Портфоліо для нових клієнтів

**Менеджер Максим:**

1. Збирає найкращі фото з усіх об'єктів
2. "🏗️ Фото Об'єктів" → Фільтр "Всі"
3. Вибирає топ-10 найвражаючих "До/Після"
4. Використовує для презентації потенційним клієнтам
5. Показує якість та обсяг виконаних робіт

---

### Сценарій 3: Документування проблеми

**Менеджер Іван:**

1. Виявлена проблема на об'єкті "Квартира №25"
2. Додає фото:
   - "Тріщина на стіні - виявлено 20.01"
   - "Підтікання під раковиною"
3. Використовує для комунікації з підрядниками
4. Після виправлення додає:
   - "Тріщина усунена - 22.01"
   - "Підтікання усунено"

---

## 🔧 Технічні Деталі

### Зберігання

**Формат:** JSONB масив в PostgreSQL

**Переваги:**
- ✅ Необмежена кількість фото
- ✅ Гнучка структура (можна додавати поля)
- ✅ Швидкі запити (GIN індекс)
- ✅ Атомарні операції

**Приклад структури:**
```json
{
  "id": "obj-123",
  "name": "Квартира №15",
  "photos": [
    {
      "url": "data:image/jpeg;base64,...",
      "label": "Фасад будівлі",
      "uploadedBy": "manager-1",
      "uploadedAt": "2024-01-15T10:30:00.000Z"
    }
  ],
  "photosUpdatedAt": "2024-01-15T10:30:00.000Z"
}
```

---

### SQL Функції

**Додати фото:**
```sql
SELECT add_object_photo(
  'object-id',
  'data:image/jpeg;base64,...',
  'Фасад будівлі',
  'manager-user-id'
);
```

**Видалити фото (по індексу):**
```sql
SELECT remove_object_photo('object-id', 0); -- видалити перше фото
```

**Кількість фото:**
```sql
SELECT object_photos_count('object-id'); -- Returns: 3
```

**Чи є фото:**
```sql
SELECT object_has_photos('object-id'); -- Returns: true/false
```

**Статистика:**
```sql
SELECT * FROM get_object_photos_statistics();
-- Returns:
-- total_objects | objects_with_photos | total_photos | coverage_percent | avg_photos_per_object
-- 25            | 15                  | 45           | 60.00            | 3.00
```

**Всі фото менеджера:**
```sql
SELECT * FROM get_all_object_photos('manager-id');
-- Returns таблицю з:
-- object_id, object_name, object_address, photo_url, photo_label, uploaded_by, uploaded_by_name, uploaded_at
```

---

### RLS Політики

**Менеджери бачать фото своїх об'єктів:**
```sql
CREATE POLICY "Managers can view photos of their objects"
ON objects FOR SELECT
USING (
  EXISTS (
    SELECT 1 FROM users
    WHERE id = auth.uid()::text
    AND (role = 'manager' OR manager_id = objects.manager_id)
  )
);
```

**Менеджери можуть оновлювати фото:**
```sql
CREATE POLICY "Managers can update photos of their objects"
ON objects FOR UPDATE
USING (
  EXISTS (
    SELECT 1 FROM users
    WHERE id = auth.uid()::text
    AND role = 'manager'
    AND (id = objects.manager_id OR objects.manager_id IS NULL)
  )
);
```

---

## 📊 Статистика

### Створено Файлів

**Нові компоненти:** 2
- `ObjectPhotosManager.tsx` (11.2 KB)
- `ObjectPhotosGallery.tsx` (7.8 KB)

**Оновлено компоненти:** 2
- `ManageObjectsModal.tsx` (+кнопка і модалка)
- `ManagerStats.tsx` (+кнопка і state)

**SQL:** 1
- `008_add_object_photos.sql` (9.8 KB)

**Типи:** 1
- `types/index.ts` (+ObjectPhoto interface)

**Документація:** 1
- `ФОТО_ОБЄКТІВ_ДОКУМЕНТАЦІЯ.md` (цей файл)

---

### Рядки Коду

```
ObjectPhotosManager.tsx    : ~380 рядків
ObjectPhotosGallery.tsx    : ~210 рядків
008_add_object_photos.sql  : ~320 рядків
ManageObjectsModal.tsx     : +30 рядків
ManagerStats.tsx           : +15 рядків
types/index.ts             : +8 рядків
─────────────────────────────────────
ВСЬОГО                     : ~960 рядків
```

---

## 🚀 Запуск

### Крок 1: Виконати SQL Міграцію ⚠️

```bash
1. Відкрийте Supabase Dashboard
   https://zdfogogoqnsdinjxkdtl.supabase.co

2. SQL Editor → New Query

3. Відкрийте файл:
   /app/supabase/migrations/008_add_object_photos.sql

4. Скопіюйте весь код (Ctrl+A, Ctrl+C)

5. Вставте в SQL Editor (Ctrl+V)

6. Натисніть RUN або F5

7. Перевірте успішне виконання ✅
```

### Крок 2: Готово! ✅

Всі компоненти вже в коді та готові до використання.

### Крок 3: Тестування 🧪

**Як менеджер:**

1. Увійдіть як менеджер
2. "Керування Об'єктами"
3. Клік "📸 Фото" на будь-якому об'єкті
4. Додайте кілька фото з підписами
5. Збережіть
6. Перейдіть в "🏗️ Фото Об'єктів"
7. Побачите всі додані фото
8. Спробуйте фільтрацію
9. Клік на фото для збільшення

---

## 💡 Поради

### Для Менеджерів

✅ **DO:**
- Додавайте фото на всіх етапах: До → Прогрес → Після
- Використовуйте описові підписи: "Фасад - південна сторона"
- Робіть фото при хорошому освітленні
- Додавайте фото регулярно (щотижня)
- Використовуйте для звітів клієнтам

❌ **DON'T:**
- Не завантажуйте розмиті або темні фото
- Не забувайте підписувати фото
- Не додавайте фото з особистою інформацією
- Не видаляйте фото без необхідності

---

## 🔥 Відмінності від Фото Процесів

| Характеристика | Фото Процесів | Фото Об'єктів |
|---------------|--------------|--------------|
| **Хто додає** | Працівники | Менеджери |
| **Коли** | При виконанні процесу | Будь-коли |
| **Кількість** | 3 фото (До/Під час/Після) | Необмежено |
| **Підписи** | Автоматичні (назва етапу) | Довільні |
| **Прив'язка** | До конкретного процесу | До об'єкта загалом |
| **Доступ** | Працівник бачить свої | Менеджер бачить всі |
| **База даних** | `processes.photo_*` (TEXT) | `objects.photos` (JSONB) |
| **Призначення** | Контроль якості робіт | Звітність клієнтам |
| **Редагування** | Тільки при створенні | Можна редагувати підписи |
| **Видалення** | Можна видалити процес | Можна видаляти окремі фото |

---

## 🎯 Use Cases

### 1. Звітність Клієнтам 📊
- Показати прогрес робіт
- До/Після порівняння
- Доведення виконання

### 2. Портфоліо 🏆
- Кращі роботи для презентації
- Приклади для нових клієнтів
- Демонстрація можливостей

### 3. Документування 📝
- Стан об'єкта на старті
- Виявлені проблеми
- Етапи виконання

### 4. Внутрішній Контроль 🔍
- Перевірка стану об'єктів
- Виявлення відхилень
- Планування ресурсів

### 5. Комунікація 💬
- З підрядниками
- З постачальниками
- З інспекторами

---

## ✅ Чеклист Впровадження

- [ ] Виконати SQL міграцію `008_add_object_photos.sql`
- [ ] Перевірити RLS політики в Supabase
- [ ] Протестувати додавання фото
- [ ] Протестувати редагування підписів
- [ ] Протестувати видалення фото
- [ ] Протестувати галерею фото об'єктів
- [ ] Протестувати фільтрацію
- [ ] Навчити менеджерів користуватись
- [ ] Створити приклади підписів для стандартизації
- [ ] (Опціонально) Налаштувати Supabase Storage

---

## 🎉 Результат

**Що отримали:**

✅ **Для Менеджерів:**
- Керування фото кожного об'єкта
- Необмежена кількість фото
- Довільні підписи
- Централізована галерея всіх фото
- Фільтрація по об'єктах
- Редагування та видалення

✅ **Для Бізнесу:**
- Прозорість для клієнтів
- Портфоліо робіт
- Документування процесів
- Захист від претензій
- Маркетингові матеріали

✅ **Для Клієнтів:**
- Візуальні звіти
- Впевненість в якості
- Розуміння прогресу
- Доказ виконання

---

**🚀 Готово до використання!**

Фото об'єктів повністю інтегровані в додаток і готові допомагати в звітності та комунікації з клієнтами!
